import { Borders } from "../common";

const Modal = {
	baseStyle: {
		dialog: {
			alignSelf: "center",
			borderRadius: Borders.md,
			// padding: "4px",
		},
		header: {
			textAlign: "center",
		},
		closeButton: {
			borderRadius: Borders.md,
		},
	},
};

export default Modal;
